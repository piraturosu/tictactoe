let CANVAS = [null, null, null, null, null, null, null, null, null];
let PLAYERS = { playerOne: 0, playerTwo: 1 };
let CHOICE = { 0: 'X', 1: 'O' };
console.log(CANVAS.length);

function storeClickedId(clickedId) {
  let choice = { playerOne: clickedId };
}

function insertPlayerChoice(playerOneChoice, playerTwoChoice) {}

function gameCompletion() {}
